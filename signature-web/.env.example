# Optional verifier gate.
VITE_VERIFIER_LOGIN_REQUIRED=false

# Netlify function secrets. Set these in Netlify environment variables,
# not in a committed .env file.
VERIFIER_USERNAME=verifier
VERIFIER_PASSWORD=change-me
